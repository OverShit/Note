linux内存管理:惰性管理

页的概念：
    页(page)：指线性地址和包含于其中的数据（即数据）
        页只是一组数据块，可以存放在任何页框中 

    页框(pageframe): (指物理内存)
        分页单元认为所有的RAM被分成固定长度的页框
        每个页框可以包含一页，也就是是一个页框的长度和一个页的长度是一样的
        页框是内存的一部分，是一个实际的存储区域
                  
    即页对应的是线性地址的东西，而页框对应的是物理地址，是实际的存储区域

伙伴算法：
    
